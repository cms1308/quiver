---
phase: 02-two-node-summary
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [results/two_node_tables.py, results/two_node_validation.md]
interactive: false

conventions:
  units: "natural (hbar=c=1)"
  gauge_groups: "SU(N), SO(N), Sp(N)=USp(2N)"
  dynkin_index: "T(fund_SU)=1/2, T(V_SO)=1, T(f_Sp)=1/2"
  central_charges: "a/N^2, c/N^2 as O(1) quantities at leading large-N order"

dimensional_check:
  a_over_N2: "[dimensionless O(1)]"
  c_over_N2: "[dimensionless O(1)]"
  a_over_c: "[dimensionless O(1), range 1/2 to 3/2 for unitary theories]"

approximations:
  - name: "large N leading order"
    parameter: "1/N"
    validity: "N >> 1"
    breaks_when: "finite N corrections matter"
    check: "a/N^2 and c/N^2 are O(1) constants independent of N"

estimated_execution:
  total_minutes: 25
  breakdown:
    - task: 1
      minutes: 15
      note: "Physics consistency checks + Hofman-Maldacena + exact-numerical"
    - task: 2
      minutes: 10
      note: "Decoupled-node limiting case + summary report"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made: []

contract:
  scope:
    question: "Are the two-node classification data internally consistent and physically valid?"
  claims:
    - id: claim-two-node-validated
      statement: "All 326 two-node universality classes have validated superconformal data: 285 unitary classes satisfy HM bounds, 10 Type-I have a=c=0, 31 non-unitary have a < 0. All pass exact-numerical consistency and c-a relation checks."
      deliverables: [deliv-validation-report]
      acceptance_tests: [test-hm-bounds-full, test-exact-vs-numerical, test-c-a-relation]
      references: [ref-hofman-maldacena, ref-phase1-data]
  deliverables:
    - id: deliv-validation-report
      kind: report
      path: "results/two_node_validation.md"
      description: "Validation report documenting all checks performed, their results, and any anomalies found"
  references:
    - id: ref-hofman-maldacena
      kind: paper
      locator: "Hofman-Maldacena 0803.1467"
      role: benchmark
      why_it_matters: "Universal bound 1/2 <= a/c <= 3/2 for any 4D SCFT"
      applies_to: [claim-two-node-validated]
      must_surface: true
      required_actions: [compare]
    - id: ref-phase1-data
      kind: prior_artifact
      locator: "results/single_node_classification.json"
      role: benchmark
      why_it_matters: "Single-node data provides decoupled-limit check for two-node theories with 0 bifundamentals"
      applies_to: [claim-two-node-validated]
      must_surface: true
      required_actions: [compare]
    - id: ref-plan01-json
      kind: prior_artifact
      locator: "results/two_node_classification.json"
      role: definition
      why_it_matters: "Plan 01 output being validated"
      applies_to: [claim-two-node-validated]
      must_surface: true
      required_actions: [read]
  acceptance_tests:
    - id: test-hm-bounds-full
      subject: claim-two-node-validated
      kind: benchmark
      procedure: "For all 285 unitary classes (a > 0), verify 1/2 <= a/c <= 3/2. For 10 Type-I classes, verify |a| < 1e-8 and |c| < 1e-8. For 31 non-unitary classes (a < 0), verify they are flagged with unitary=false and document their a/c values and HM violation status."
      pass_condition: "All 285 classes with a > 0 satisfy 1/2 <= a/c <= 3/2. 10 Type-I classes have |a| < 1e-8 and |c| < 1e-8. 31 classes with a < 0 flagged as non-unitary (13 violate HM bounds, 25 have c < 0)."
      evidence_required: [ref-plan01-json, ref-hofman-maldacena]
    - id: test-exact-vs-numerical
      subject: claim-two-node-validated
      kind: consistency
      procedure: "For all 181 classes with exact symbolic data: (1) parse a_exact and c_exact as sympy expressions, (2) evaluate float(), (3) compare with stored a_over_N2 and c_over_N2."
      pass_condition: "All 181 classes: |float(exact) - numerical| < 1e-8 for both a and c."
      evidence_required: [deliv-validation-report, ref-plan01-json]
    - id: test-c-a-relation
      subject: claim-two-node-validated
      kind: consistency
      procedure: "Verify c - a = -(1/16)(Tr R - Tr R^3) by computing c_over_N2 - a_over_N2 and comparing with -(1/16)(Tr_R - Tr_R3) derived from R-charges. For exact classes, verify algebraically; for numerical, verify to 1e-8."
      pass_condition: "c - a relation holds for all 326 classes."
      evidence_required: [deliv-validation-report]
  forbidden_proxies:
    - id: fp-spot-check-only
      subject: claim-two-node-validated
      proxy: "Checking only a handful of classes rather than all 326"
      reason: "Systematic errors (e.g., convention mismatch) would only be caught by exhaustive checking"
  links:
    - id: link-plan01-to-validation
      source: ref-plan01-json
      target: deliv-validation-report
      relation: "benchmarks"
      verified_by: [test-hm-bounds-full, test-exact-vs-numerical, test-c-a-relation]
  uncertainty_markers:
    weakest_anchors:
      - "Decoupled-limit test is inapplicable: DB contains zero theories with N_bif=0 (quiver generation enforces at least one connecting edge). This limiting case cannot be tested."
      - "c-a relation test requires parsing R-charge strings to reconstruct traces"
    disconfirming_observations:
      - "Any HM bound violation among the 285 unitary (a > 0) classes would indicate error in a-maximization"
      - "Systematic exact-numerical mismatch pattern would indicate a code bug in the symbolic pipeline"
      - "Non-unitary classes (a < 0) are expected — they are a physical finding, not a computation error"
---

<objective>
Validate the complete two-node classification data against physics consistency checks: Hofman-Maldacena bounds, exact vs numerical agreement, c-a identity, and decoupled-node limiting case.

Purpose: Ensures the classification tables from Plan 01 are physically correct before use in later phases (distribution analysis, paper writing).
Output: Validation script additions to results/two_node_tables.py, validation report results/two_node_validation.md.
</objective>

<execution_context>
@/Users/cms1308/.claude/get-physics-done/workflows/execute-plan.md
@/Users/cms1308/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/01-single-node-summary/01-01-SUMMARY.md
@results/two_node_classification.json
@results/single_node_classification.json
@quivers.db
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run exhaustive physics consistency checks</name>
  <files>results/two_node_tables.py</files>
  <action>
    Add a validation section to results/two_node_tables.py that performs ALL of the following checks on every class:

    Check 1 -- Hofman-Maldacena bounds and unitarity:
    Tier A (285 unitary, a > 0): verify 1/2 <= a/c <= 3/2 and c/N^2 > 0.
    Tier B (10 Type-I): verify |a/N^2| < 1e-8 and |c/N^2| < 1e-8.
    Tier C (31 non-unitary, a < 0): verify flagged as non-unitary. Record a/c values and count HM violations (expect 13). Record how many have c < 0 (expect 25).
    Record any unexpected violations (unitary class failing HM, or wrong tier count).

    Check 2 -- Exact vs numerical consistency:
    For each of the 181 classes with exact symbolic data (a_exact, c_exact in DB):
    - Parse a_exact and c_exact as sympy expressions
    - Evaluate to float
    - Compare with stored numerical a_over_N2 and c_over_N2
    - Flag any |exact - numerical| > 1e-8

    Check 3 -- c-a relation:
    The identity c - a = -(1/16)(Tr R - Tr R^3) provides an independent check.
    For classes with exact symbolic R-charges:
    - Parse R_exact strings to extract individual R-charge values
    - Compute Tr R and Tr R^3 from R-charges and field multiplicities
    - Verify c_over_N2 - a_over_N2 = -(1/16)(Tr_R - Tr_R3)
    If R-charge parsing is too fragile, fall back to: verify c = (1/32)(9*Tr_R3 - 5*Tr_R) and a = (3/32)(3*Tr_R3 - Tr_R) give the same c-a as stored values, using the representative theory's numerical R-charges.

    Check 4 -- Tier consistency and sanity:
    - 285 unitary: a_over_N2 > 0 and c_over_N2 > 0 and 1/2 <= a/c <= 3/2
    - 10 Type-I: |a_over_N2| < 1e-8 AND |c_over_N2| < 1e-8 (both genuinely zero)
    - 31 non-unitary: a_over_N2 < 0, flagged with unitary=false
    - Among non-unitary: 25 have c < 0, 13 violate HM bounds
    - Tier counts sum to 326: 285 + 10 + 31 = 326

    Check 5 -- Theory count consistency:
    Sum of n_theories across all classes = total classified theories in theory table.
    7757 total theories - 220 unclassified = expected sum.

    Report: print summary of checks run, pass/fail counts, and any flagged items.
  </action>
  <verify>
    1. Zero HM violations among 285 unitary classes; 13 HM violations among 31 non-unitary classes documented
    2. Zero exact-numerical mismatches > 1e-8
    3. c-a relation holds for all tested classes
    4. Tier counts: 285 unitary + 10 Type-I + 31 non-unitary = 326
    5. Theory counts consistent
    6. All checks run on ALL classes (not a subset)
  </verify>
  <done>All physics consistency checks pass for all 326 classes. 285 unitary classes satisfy HM bounds. 31 non-unitary classes documented with a < 0. 10 Type-I confirmed at a=c=0. Exact-numerical agreement confirmed for 181 exact classes.</done>
</task>

<task type="auto">
  <name>Task 2: Decoupled-node limit check and validation report</name>
  <files>results/two_node_validation.md</files>
  <action>
    Part A -- Decoupled-node limiting case:
    Document as inapplicable: the DB contains zero theories with N_bif=0. The quiver generation enforces at least one connecting edge between the two nodes, so the decoupled-node limit cannot be tested within this dataset. Note this in the validation report as a known limitation.

    Part B -- Write validation report:
    Produce results/two_node_validation.md summarizing:

    1. Data completeness:
       - 326/326 classes with a/N^2 (100%)
       - 326/326 classes with c/N^2 (100%: 181 exact + 145 numerical)
       - 316/326 classes with a/c defined (96.9%: 10 Type-I have a/c undefined)
       - 326/326 classes with R-charges (181 exact + 145 numerical)

    2. Three-tier classification:
       - 285 unitary (a > 0): flow to unitary SCFTs, all satisfy HM bounds
       - 10 Type-I (a=c=0): a/c undefined, degenerate fixed points
       - 31 non-unitary (a < 0): do not flow to unitary SCFTs (a-theorem), 13 violate HM bounds, 25 have c < 0

    3. Physics checks (with pass/fail counts):
       - Hofman-Maldacena bounds: 285/285 unitary pass; 13/31 non-unitary violate (expected)
       - Exact-numerical agreement: X/181 pass
       - c-a relation: X/N pass (N = number tested)
       - Decoupled limit: not applicable (N_bif >= 1 for all theories)
       - Theory count: consistent/inconsistent

    4. Summary statistics by gauge pair type:
       Table with columns: gauge_pair | n_classes | n_unitary | n_type_I | n_non_unitary | non_unitary_frac | n_exact | a/N^2 range | c/N^2 range | a/c range (unitary only)

    5. Notable observations:
       - Which gauge pairs have the most classes? Fewest?
       - Non-unitary fraction per gauge pair (which gauge combinations are most likely to produce non-unitary theories?)
       - Any interesting patterns in a/c distribution among unitary classes?
       - Fraction with exact symbolic data by gauge pair

    Total check count and pass rate.
  </action>
  <verify>
    1. Validation report covers all check categories including three-tier breakdown
    2. Check counts sum correctly (285 + 10 + 31 = 326)
    3. Summary statistics are consistent with the JSON data
    4. Decoupled-limit documented as inapplicable (N_bif >= 1 for all theories)
    5. Non-unitary fraction reported per gauge pair
    6. Report is self-contained (readable without running the script)
  </verify>
  <done>Validation report produced with all checks documented. Total pass rate reported. Limiting case either verified or explained as inapplicable. Summary statistics by gauge pair compiled.</done>
</task>

</tasks>

<verification>
- Three-tier decomposition validated: 285 unitary + 10 Type-I + 31 non-unitary = 326
- All 285 unitary classes pass Hofman-Maldacena bounds (1/2 <= a/c <= 3/2)
- 31 non-unitary classes documented (a < 0, 13 violate HM, 25 have c < 0)
- All 181 exact classes pass exact-numerical consistency (tolerance 1e-8)
- Decoupled-node limit documented as inapplicable (N_bif >= 1)
- Non-unitary fraction per gauge pair reported
- Total validation count and pass rate reported
</verification>

<success_criteria>
All 285 unitary classes pass HM bounds. 31 non-unitary and 10 Type-I classes correctly classified. Zero unexpected violations. Validation report documents complete three-tier audit trail. All data from Plan 01 confirmed as physically consistent and ready for downstream use in Phases 3-6.
</success_criteria>

<output>
After completion, create `.gpd/phases/02-two-node-summary/02-02-SUMMARY.md`
</output>
