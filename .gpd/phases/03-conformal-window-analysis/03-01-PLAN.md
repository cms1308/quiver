---
phase: 03-conformal-window-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [results/conformal_window.py, results/conformal_window_classification.json, results/tables/conformal_window.md]
interactive: false

conventions:
  units: "natural (hbar=c=1)"
  gauge_groups: "SU(N), SO(N), Sp(N)=USp(2N)"
  dynkin_index: "T(fund_SU)=1/2, T(V_SO)=1, T(f_Sp)=1/2"
  conformal_window_parameter: "x_a = N_bif * T_bifund_lead(ga,gb)[a] * m_b / m_a"
  gauge_pair_ordering: "alphabetical (SO-SO, SO-Sp, SU-SO, SU-SU, SU-Sp, Sp-Sp)"

dimensional_check:
  x_a: "[dimensionless]"
  b_0_coefficient: "[dimensionless] (3 - x_a)"

approximations:
  - name: "large-N leading order"
    parameter: "1/N"
    validity: "N >> 1"
    breaks_when: "finite-N corrections to T(adj) or T(bifund) become relevant"
    check: "x < 3 for all AF morphologies (AF already enforced in Phase 2)"
  - name: "per-node SQCD analogy"
    parameter: "inter-node coupling ignored"
    validity: "when each node's one-loop beta is dominated by its own matter content"
    breaks_when: "coupled RG effects modify conformal window boundaries"
    check: "this is a necessary condition only; note explicitly in deliverables"

estimated_execution:
  total_minutes: 25
  breakdown:
    - task: 1
      minutes: 15
      note: "Query DB, compute x, classify, generate JSON + markdown + summary stats"
    - task: 2
      minutes: 10
      note: "SQCD limit verification, AF consistency, cross-checks, physics interpretation"

contract:
  scope:
    question: "Which N_rank2=0 nodes in the two-node quiver classification lie inside the SQCD conformal window, and what physics does this reveal about the landscape?"
  claims:
    - id: claim-conformal-window
      statement: "Conformal window analysis identifies which N_rank2=0 nodes lie inside/outside the SQCD conformal window at large N, using the general formula x_a = N_bif * T_bifund_lead(ga,gb)[a] * m_b / m_a"
      deliverables: [deliv-conformal-window-table, deliv-conformal-window-json]
      acceptance_tests: [test-sqcd-limit, test-af-consistency, test-su-su-symmetry]
      references: [ref-seiberg-sqcd, ref-phase2-data, ref-t-bifund-lead]
  deliverables:
    - id: deliv-conformal-window-json
      kind: data
      path: "results/conformal_window_classification.json"
      description: "Machine-readable JSON with per-node x values and inside/outside/boundary classification for every N_rank2=0 node"
      must_contain: ["morph_id", "node_index", "gauge_type", "x_value", "classification", "N_bif", "gauge_pair"]
    - id: deliv-conformal-window-table
      kind: table
      path: "results/tables/conformal_window.md"
      description: "Markdown table of conformal window classification with distribution analysis by gauge pair"
      must_contain: ["x value", "inside/outside classification", "distribution summary", "physics interpretation"]
    - id: deliv-conformal-window-script
      kind: code
      path: "results/conformal_window.py"
      description: "Self-contained Python script that queries quivers.db, computes x for all N_rank2=0 nodes, classifies, validates, and produces JSON + markdown output"
      must_contain: ["T_bifund_lead lookup", "x computation with rank multipliers", "SQCD limit check", "AF consistency check"]
  references:
    - id: ref-seiberg-sqcd
      kind: paper
      locator: "Seiberg, hep-th/9411149; Intriligator-Seiberg, hep-th/9503179; Intriligator-Pouliot, hep-th/9505006"
      aliases: ["Seiberg duality", "SQCD conformal window"]
      role: definition
      why_it_matters: "Defines the conformal window 3/2 < x < 3 for SU, SO, Sp gauge theories. The SQCD limit is the primary validation benchmark."
      applies_to: [claim-conformal-window]
      carry_forward_to: [verification, writing]
      must_surface: true
      required_actions: [cite, compare]
    - id: ref-phase2-data
      kind: prior_artifact
      locator: "results/two_node_classification.json, quivers.db morphology_class table"
      role: definition
      why_it_matters: "Source data for all 471 morphologies with N_rank2 values, N_bif, gauge_pair, rank multipliers"
      applies_to: [claim-conformal-window]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [read, use]
    - id: ref-t-bifund-lead
      kind: prior_artifact
      locator: "a_maximization_large_N.py lines 116-137"
      role: definition
      why_it_matters: "Provides T_bifund_lead(ga,gb) for all 9 gauge pairs -- essential for correct x computation"
      applies_to: [claim-conformal-window]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [use]
  acceptance_tests:
    - id: test-sqcd-limit
      subject: claim-conformal-window
      kind: limiting_case
      procedure: "Verify that for a single SU(N) node with N_f fundamental flavors (equivalent to SU-SU quiver with N_bif=2*N_f/N and one node having N_rank2=0), x = N_f/N reproduces the Seiberg conformal window 3/2 < N_f/N < 3. Concretely: SU-SU with N_bif=4 gives x=2 (inside), N_bif=3 gives x=3/2 (boundary), N_bif=6 gives x=3 (AF boundary)."
      pass_condition: "SQCD conformal window exactly reproduced: x=N_bif/2 for SU-SU, with 3/2 < x < 3 matching 3N_c/2 < N_f < 3N_c in Seiberg normalization"
      evidence_required: [deliv-conformal-window-json, ref-seiberg-sqcd]
      automation: automated
    - id: test-af-consistency
      subject: claim-conformal-window
      kind: consistency
      procedure: "Verify x < 3 for every N_rank2=0 node in the database. x >= 3 would contradict asymptotic freedom, which is already enforced."
      pass_condition: "All x values strictly less than 3"
      evidence_required: [deliv-conformal-window-json, ref-phase2-data]
      automation: automated
    - id: test-su-su-symmetry
      subject: claim-conformal-window
      kind: symmetry
      procedure: "For SU-SU morphologies with both nodes having N_rank2=0 and equal rank multipliers, verify x_0 = x_1 (both nodes see the same effective number of flavors)"
      pass_condition: "x_0 = x_1 for all symmetric SU-SU morphologies"
      evidence_required: [deliv-conformal-window-json]
      automation: automated
  forbidden_proxies:
    - id: fp-tables-without-structure
      subject: claim-conformal-window
      proxy: "Raw classification table listing inside/outside without distribution analysis or physics interpretation"
      reason: "Tables alone are a catalog, not physics. Must include: (1) distribution of x values by gauge pair, (2) fraction inside window by gauge pair, (3) comparison of which gauge pairs are more/less likely to be inside, (4) statement that this is a necessary condition only"
    - id: fp-wrong-x-formula
      subject: claim-conformal-window
      proxy: "Using x = N_bif/2 universally instead of the general formula with T_bifund_lead and rank multipliers"
      reason: "Would give incorrect classification for non-SU-SU gauge pairs, especially SO-Sp where T_lead=2 at the SO node"
  links:
    - id: link-classification-to-table
      source: claim-conformal-window
      target: deliv-conformal-window-table
      relation: supports
      verified_by: [test-sqcd-limit, test-af-consistency]
    - id: link-json-to-claim
      source: claim-conformal-window
      target: deliv-conformal-window-json
      relation: computes
      verified_by: [test-sqcd-limit, test-af-consistency, test-su-su-symmetry]
  uncertainty_markers:
    weakest_anchors:
      - "Per-node SQCD analogy ignores coupled RG effects between the two nodes -- conformal window classification is necessary but not sufficient for IR fixed point existence"
    disconfirming_observations:
      - "If any morphology known to have an IR fixed point (from Module 4 analysis) has an N_rank2=0 node classified as outside the conformal window, the criterion would be invalidated as a necessary condition"
      - "If x >= 3 for any N_rank2=0 node in an AF theory, the T_bifund_lead values or rank multiplier handling is wrong"
---

<objective>
Classify all N_rank2=0 nodes in the two-node quiver database by the SQCD conformal window criterion 3/2 < x < 3, where x_a = N_bif * T_bifund_lead(ga,gb)[a] * m_b / m_a.

Purpose: Determine which nodes with only bifundamental matter (no rank-2 tensors) lie inside the conformal window, providing a necessary condition for non-trivial IR fixed points. This is the first physics filter beyond asymptotic freedom.
Output: Python script (results/conformal_window.py), JSON classification (results/conformal_window_classification.json), markdown table with distribution analysis (results/tables/conformal_window.md).
</objective>

<execution_context>
@/Users/cms1308/.claude/get-physics-done/workflows/execute-plan.md
@/Users/cms1308/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/03-conformal-window-analysis/03-RESEARCH.md
@.gpd/phases/02-two-node-summary/02-01-SUMMARY.md
@a_maximization_large_N.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute conformal window classification for all N_rank2=0 nodes</name>
  <files>results/conformal_window.py, results/conformal_window_classification.json, results/tables/conformal_window.md</files>
  <action>
    Create a self-contained Python script results/conformal_window.py that:

    1. DEFINE T_bifund_lead lookup (inline, matching a_maximization_large_N.py lines 116-137):
       - SU-SU: (1/2, 1/2)
       - SU-SO: (1/2, 1)
       - SU-Sp: (1, 1/2)
       - SO-SO: (1, 1)
       - SO-Sp: (2, 1/2)
       - Sp-Sp: (1, 1)
       Use fractions.Fraction for exact rational arithmetic throughout.

    2. QUERY quivers.db morphology_class table:
       SELECT morph_id, gauge_pair, rank0_mult, rank1_mult, N_rank2_0, N_rank2_1, N_bif, N_fund_0, N_fund_1
       FROM morphology_class
       WHERE N_rank2_0 = 0 OR N_rank2_1 = 0

    3. For EACH qualifying row, for EACH node index (0 or 1) where N_rank2_{node}=0:
       - Parse gauge_pair string "GA-GB" to get gauge types (node 0 = GA, node 1 = GB)
       - Look up T_lead for this node: T_bifund_lead(GA, GB)[node_index]
       - Get rank multipliers: m_self = rank{node}_mult, m_other = rank{other_node}_mult
       - Compute x = N_bif * T_lead * m_other / m_self (as exact Fraction)
       - Classify:
         * "inside" if 3/2 < x < 3
         * "at_lower_boundary" if x = 3/2
         * "at_upper_boundary" if x = 3
         * "below" if x < 3/2 (confining regime)
         * "above" if x > 3 (should NOT occur for AF theories -- flag as ERROR)

    4. VALIDATE (automated checks, print PASS/FAIL for each):
       a. AF consistency: x < 3 for all entries (x=3 means b_0=0, which is marginal, not AF; but the database might include marginal cases at the AF boundary -- report x=3 separately)
       b. SU-SU symmetry: for SU-SU morphologies with both nodes N_rank2=0 and rank0_mult=rank1_mult, verify x_0 = x_1
       c. SQCD limit: verify SU-SU with N_bif=4 gives x=2 (inside), N_bif=3 gives x=3/2 (boundary), N_bif=6 gives x=3 (upper boundary)
       d. Count check: total morphologies with at least one N_rank2=0 node should be 199

    5. OUTPUT JSON (results/conformal_window_classification.json):
       Array of objects, one per (morph_id, node_index) pair:
       {
         "morph_id": int,
         "node_index": 0 or 1,
         "gauge_pair": string,
         "gauge_type": string (gauge type of this specific node),
         "rank_self": int,
         "rank_other": int,
         "N_bif": int,
         "T_lead": string (fraction as "p/q"),
         "x": string (fraction as "p/q"),
         "x_float": float,
         "classification": "inside"|"below"|"at_lower_boundary"|"at_upper_boundary"|"above"
       }

    6. OUTPUT MARKDOWN (results/tables/conformal_window.md):
       - Title: "# Conformal Window Classification of N_rank2=0 Nodes"
       - Section: "## Summary" with total counts by classification category
       - Section: "## Distribution by Gauge Pair" -- for EACH gauge pair, a sub-table showing:
         * Number of qualifying nodes (not morphologies)
         * Breakdown: inside / below / boundary
         * Distinct x values that appear
       - Section: "## Classification Table" -- full table sorted by gauge_pair then x
       - Section: "## Physics Interpretation" containing:
         * Statement that this is a necessary condition only (per-node SQCD analogy)
         * Which gauge pairs have the highest/lowest fraction inside the window
         * How rank multipliers shift the classification (rank(1,2) vs rank(1,1) examples)
         * Connection to the SQCD conformal window literature (Seiberg, Intriligator-Seiberg, Intriligator-Pouliot)

    CRITICAL: Do NOT use x = N_bif/2 for all gauge types. Use the general formula with T_bifund_lead and rank multipliers. The research document (03-RESEARCH.md) explains why and provides the correct T_bifund_lead table.
  </action>
  <verify>
    1. x < 3 for all entries (AF consistency)
    2. Total morphologies with N_rank2=0 node(s) = 199
    3. SU-SU symmetry: x_0 = x_1 when both nodes qualify and ranks are equal
    4. SQCD limit: SU-SU N_bif=4 -> x=2, N_bif=3 -> x=3/2, N_bif=6 -> x=3
    5. All x values are exact rationals (no floating-point approximation in classification logic)
    6. JSON has one entry per (morph_id, node_index) qualifying pair
    7. Markdown includes distribution analysis and physics interpretation (not just raw table)
  </verify>
  <done>Script produces validated JSON and markdown with all N_rank2=0 nodes classified, all 4 automated checks pass, distribution analysis and physics interpretation included in markdown</done>
</task>

<task type="auto">
  <name>Task 2: Cross-validate T_bifund_lead and verify SQCD limits for all gauge types</name>
  <files>results/conformal_window.py</files>
  <action>
    Add a dedicated validation section to the script (or run as a separate validation block) that:

    1. VERIFY T_bifund_lead against first-principles Dynkin index computation:
       For each gauge pair (GA, GB), the bifundamental representation is (fund_A, fund_B).
       At node A: T_A(bifund) = dim(fund_B) * T(fund_A).
       At large N:
       - dim(fund_SU(mN)) = mN, dim(V_SO(mN)) = mN, dim(f_Sp(mN)) = 2mN
       - T(fund_SU) = 1/2, T(V_SO) = 1, T(f_Sp) = 1/2

       So T_lead_A = dim_lead(fund_B) * T(fund_A) where dim_lead is the coefficient of N:
       - SU(mN) fund: dim_lead = m
       - SO(mN) vector: dim_lead = m
       - Sp(mN) fund: dim_lead = 2m

       Verify for each of the 6 gauge pairs that the T_bifund_lead values match this derivation.
       For example, SU-Sp: T_lead at SU node = dim_lead(f_Sp) * T(fund_SU) = 2 * 1/2 = 1. Correct.
       SO-Sp: T_lead at SO node = dim_lead(f_Sp) * T(V_SO) = 2 * 1 = 2. Correct.

    2. VERIFY SQCD conformal window for each gauge type at large N:
       - SU(N_c) with N_f flavors: b_0 = 3N_c - N_f/2 * 2 = 3N_c - N_f. Window: 3N_c/2 < N_f < 3N_c, i.e., 3/2 < N_f/N_c < 3. Maps to x = N_f/N_c. Matches.
       - SO(N_c) with N_f vectors: b_0 = 3(N_c-2) - N_f at large N. Window: 3(N_c-2)/2 < N_f < 3(N_c-2). At large N: 3/2 < N_f/N_c < 3. Maps to x = N_f/N_c. Matches.
       - Sp(N_c) with 2N_f half-fundamentals: b_0 = 3(N_c+1) - N_f at large N. Window: 3(N_c+1)/2 < N_f < 3(N_c+1). At large N: 3/2 < N_f/N_c < 3. Maps to x = N_f/N_c. Matches.
       Print explicit verification for each gauge type showing the large-N limit reproduces 3/2 < x < 3.

    3. CROSS-CHECK against Phase 2 data:
       - For morphologies with BOTH nodes having N_rank2=0, verify that the total (x_0 + x_1) or the individual x values are consistent with the overall AF condition (b_0 > 0 for each node means x < 3 for each node independently).
       - Count: how many morphologies have BOTH nodes inside the conformal window? How many have exactly one? How many have none?

    4. SUMMARY STATISTICS (append to markdown):
       - Total qualifying nodes (not morphologies)
       - Breakdown: N inside, N below, N at boundary
       - By gauge pair: fraction inside
       - By rank sector: does rank(1,2) vs rank(1,1) change the classification?
       - Identify the "most common" x values and their physical meaning

    Print all validation results. Any failure should be clearly flagged.
  </action>
  <verify>
    1. T_bifund_lead values match first-principles Dynkin index derivation for all 6 gauge pairs
    2. SQCD conformal window at large N reduces to 3/2 < x < 3 for SU, SO, and Sp independently
    3. No morphology has x > 3 for any N_rank2=0 node (AF consistency)
    4. Cross-check with Phase 2: morphology count matches (199 morphologies with at least one N_rank2=0 node)
    5. Summary statistics are internally consistent (inside + below + boundary = total)
  </verify>
  <done>T_bifund_lead independently verified from Dynkin indices, SQCD limit confirmed for all 3 gauge types, cross-checks pass, complete summary statistics produced</done>
</task>

</tasks>

<verification>
- AF consistency: x < 3 for all N_rank2=0 nodes in AF theories
- SQCD limit: single SU(N) with N_f flavors gives 3/2 < N_f/N < 3 (Seiberg window)
- T_bifund_lead: independently derived from Dynkin indices, matches a_maximization_large_N.py
- SU-SU symmetry: both nodes get same x when ranks are equal
- Morphology count: 199 morphologies with at least one N_rank2=0 node (matches research)
- Classification completeness: every qualifying node has exactly one classification label
- No forbidden proxy: output includes distribution analysis and physics interpretation, not just raw tables
</verification>

<success_criteria>
All N_rank2=0 nodes classified with exact rational x values. T_bifund_lead verified from first principles. SQCD limit reproduced for SU, SO, Sp. Distribution analysis by gauge pair produced with physics interpretation. JSON and markdown deliverables complete with all required fields.
</success_criteria>

<output>
After completion, create `.gpd/phases/03-conformal-window-analysis/03-01-SUMMARY.md`.
</output>
